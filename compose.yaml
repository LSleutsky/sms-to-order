services:
  app:
    build: .
    env_file: .env
    environment:
      PORT: "3000"
      DB_PATH: /app/data/app.db
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
